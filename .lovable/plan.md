

# Update Splash Screen Text

## Change
Replace the "iView" text on the splash screen with just "i" to match the branding style.

## Technical Details

**File: `src/components/SplashScreen.tsx`**
- Change the text content inside the `<span>` element (around line 97) from `iView` to `i`
- No other changes needed -- the gradient styling, animation, and layout all remain the same

This is a single-line text change.

